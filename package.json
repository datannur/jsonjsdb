{
  "name": "jsonjsdb-monorepo",
  "private": true,
  "workspaces": [
    "jsonjsdb",
    "jsonjsdb-builder"
  ],
  "description": "A comprehensive client-side relational database solution for static Single Page Applications (SPA). Jsonjsdb enables offline data storage and querying capabilities when running applications on the local file system without requiring server infrastructure.",
  "license": "MIT",
  "author": "Bassim Matar",
  "repository": {
    "type": "git",
    "url": "https://github.com/datannur/jsonjsdb.git"
  },
  "homepage": "https://github.com/datannur/jsonjsdb#readme",
  "scripts": {
    "build": "npm run build:core && npm run build:builder",
    "build:core": "cd jsonjsdb && npm run build",
    "build:builder": "cd jsonjsdb-builder && npm run build",
    "test": "npm run test:core && npm run test:builder",
    "test:core": "cd jsonjsdb && npm run test",
    "test:builder": "cd jsonjsdb-builder && npm run test",
    "dev": "npm run dev:core",
    "dev:core": "cd jsonjsdb && npm run dev",
    "lint": "npm run lint:core && npm run lint:builder",
    "lint:core": "cd jsonjsdb && npm run lint",
    "lint:builder": "cd jsonjsdb-builder && npm run lint",
    "type-check": "npm run type-check:core && npm run type-check:builder",
    "type-check:core": "cd jsonjsdb && npx tsc --noEmit",
    "type-check:builder": "cd jsonjsdb-builder && npx tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npm run format:check && npm run lint && npm run type-check",
    "verify": "npm run check && npm run test"
  },
  "prettier": {
    "semi": false,
    "arrowParens": "avoid",
    "singleQuote": true
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.3",
    "jiti": "^2.6.1",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.57.1"
  }
}
